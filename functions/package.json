{
  "name": "functions",
  "description": "Cloud Functions for Firebase",
  "scripts": {
    "start": "firebase serve -o $IP -p $PORT --only hosting,functions"
  },
  "dependencies": {
    "firebase-admin": "^5.4.3",
    "firebase-functions": "^0.7.2",
    "riothing": "0.0.5"
  },
  "private": true
}
